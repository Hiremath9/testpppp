---
 - hosts: localhost
   become: yes
   tasks:
     - name: Run the commandssssssss
       shell:
         "uname"
       register: osname
     - name: displaying the details
       debug:
          msg: "{{ osname }}"
