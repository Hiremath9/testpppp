---
 - hosts: localhost
   become: yes
   tasks:
     - name: Run the commandssssssssgggggg
       shell:
         "uname"
       register: osname
     - name: displaying the details
       debug:
          msg: "{{ osname }}"
